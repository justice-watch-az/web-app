name: Pima JC DUI Arraignment Scraper

on:
  schedule:
    # Weekdays ~9:30 AM Arizona (UTC-7 always) — 30 min offset from Maricopa run.
    - cron: '30 16 * * 1-5'
  workflow_dispatch:

jobs:
  scrape-pima-jc:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      issues: write  # allow failure-notification issue creation

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install -r scrapers/requirements.txt

      - name: Run Pima Consolidated JC DUI arraignment scraper
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}
          SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
        run: |
          python scrapers/pima_jc_scraper.py

      - name: Notify on failure
        if: failure()
        uses: actions/github-script@v6
        with:
          script: |
            await github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: `Pima JC scraper failed: ${new Date().toLocaleDateString()}`,
              body: `Check the [workflow run](${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}).`,
              labels: ['bug', 'scraper']
            })
