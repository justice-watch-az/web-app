name: Simple Test

on:
  workflow_dispatch:
  push:
    branches:
      - feature/v3-serverless-migration

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Echo test
        run: echo "Workflow is working\!"
